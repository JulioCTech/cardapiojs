import { Space_Mono } from "next/font/google"

export const Logo = () => {
    return (
        <div className="text-xl">
            Cardápio<span className="font-bold">Web</span>
        </div>
    )
}